# Generated by: generate_scenario.py
# Parameters: {"trajectories": "/usr/uli-net-sim/uav_rid/simulations/urbanenv_testing/scenario3_waypoints.xml", "buildings": "/usr/uli-net-sim/uav_rid/simulations/urbanenv_testing/scenario3_buildings.xml", "tx_power": "10-16", "beacon_interval": "0.25-0.75", "beacon_offset": "0-0.1", "sim_time_limit": 300.0, "ghost_host": null, "spoofer_host": null, "config_name": "Scenario3_WideCorridorsLowBuildings", "seed": 3}
#
# Trajectories: /usr/uli-net-sim/uav_rid/simulations/urbanenv_testing/scenario3_waypoints.xml
# Buildings: /usr/uli-net-sim/uav_rid/simulations/urbanenv_testing/scenario3_buildings.xml

[General]
network = uav_rid.rid_network.BasicUav

*.radioMedium.obstacleLoss.typename = "DielectricObstacleLoss"

# Network node visualization
*.visualizer.osgVisualizer.networkNodeVisualizer.displayModuleName = true

# Mobility trail visualization
*.visualizer.*.mobilityVisualizer.displayMovementTrails = true
*.visualizer.*.mobilityVisualizer.trailLength = 500

# Enable vector recording for all management layer data
*.host[*].wlan[0].mgmt.statistic-recording = true


# ==============================================================================
# Base configuration for generated urban environment scenarios
# ==============================================================================
[Config UrbanEnvBase]
# Use TurtleMobility to follow waypoints from XML file
*.host[*].mobility.typename = "TurtleMobility"
*.host[*].mobility.updateInterval = 100ms

# Enable position vector recording
*.host[*].mobility.statistic-recording = true

# Default host type (can be overridden for spoofer/ghost scenarios)
*.host[*].typename = "KalmanFilterDetectHost"
*.host[*].wlan[0].mgmt.transmitBeacon = true
*.host[*].wlan[0].mgmt.startupJitter = 0ms


# ==============================================================================
# Scenario3_WideCorridorsLowBuildings
# ==============================================================================
[Config Scenario3_WideCorridorsLowBuildings]
extends = UrbanEnvBase
description = "3 hosts, with buildings"

sim-time-limit = 300s
*.numHosts = 3

*.physicalEnvironment.config = xmldoc("../scenario3_buildings.xml")

# Per-host parameters (sampled deterministically from seed)
*.host[0].wlan[0].radio.transmitter.power = 11.427788dBm
*.host[0].wlan[0].mgmt.beaconInterval = 0.522115s
*.host[0].wlan[0].mgmt.beaconOffset = 0.036996s
*.host[1].wlan[0].radio.transmitter.power = 13.623520dBm
*.host[1].wlan[0].mgmt.beaconInterval = 0.562860s
*.host[1].wlan[0].mgmt.beaconOffset = 0.006553s
*.host[2].wlan[0].radio.transmitter.power = 10.079008dBm
*.host[2].wlan[0].mgmt.beaconInterval = 0.668735s
*.host[2].wlan[0].mgmt.beaconOffset = 0.025935s

# Trajectories
*.host[0].mobility.turtleScript = xmldoc("../scenario3_waypoints.xml", "movements/movement[@id='0']")
*.host[1].mobility.turtleScript = xmldoc("../scenario3_waypoints.xml", "movements/movement[@id='1']")
*.host[2].mobility.turtleScript = xmldoc("../scenario3_waypoints.xml", "movements/movement[@id='2']")
